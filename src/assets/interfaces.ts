export interface NavMenu {
  path: string;
  label: string;
}

export interface StockData {
  companyName: string;
  primaryExchange: string;
  highSource: string;
  volume: number;
  symbol: number;
}
